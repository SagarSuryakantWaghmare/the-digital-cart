# Vercel deployment configuration
# Build settings
VERCEL_ENV=production

# Node version
VERCEL_NODE_VERSION=18.x

# Build command (this is handled by package.json but can be overridden)
# BUILD_COMMAND=npm run build

# Install command
# INSTALL_COMMAND=npm install